# React App

A simple React application, storing a list of books using props and components in React and Vite.
The application uses a header, footer and books component and the CSS is made by inline CSS, from app.css or for the BookComponent, from BookComponent.CSS.
## Run the app
```bash
npm install
npm start
